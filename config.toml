# The URL the site will be built for
base_url = "https://dynamotivation.com"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true
minify_html = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

# When set to "false", Sitemap.xml is not generated
generate_sitemap = true

# When set to "false", robots.txt is not generated
generate_robots_txt = true

generate_feeds = true
feed_filenames = ["rss.xml", "atom.xml"]

taxonomies = [
    {name = "software-categories", feed = false, render = true},
    {name = "programming-languages", feed = false, render = true}
]

[extra]
# Put all your custom variables hereF